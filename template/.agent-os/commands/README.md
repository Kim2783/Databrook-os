# Commands

This folder is for command-level orchestration guidance.

Command specifications are stored directly in this folder as `cmd-*.md` with index `commands.md`.

Suggested usage:

- Keep command playbooks and examples here.
- Add command input/output schemas as they mature.
- Track command lifecycle and version notes.
